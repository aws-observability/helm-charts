region: us-west-2
clusterName: minikube
# Test validates feature-targeted OTEL config routing (cluster-scraper vs agent),
# so explicitly opt into OTEL (off by default per v7.0.0 opt-in design).
otelContainerInsights:
  enabled: true
